# Residuals

Open items that survived their slice's fix cycles, or can't be done by
agents. Revisit at S8 (hardening) or when they block a later slice.

| # | Slice | Item | Severity | Why parked |
|---|---|---|---|---|
| R1 | infra | ~~GitHub Pages not enabled~~ RESOLVED 2026-09-22: repo made public (user decision), Pages enabled, deploy green, live at https://janfredrics.github.io/pineapple-run/ | — | done |
| R2 | S0 | Real-phone stability/60fps verification of the spike (agents can only verify in desktop browser). Spike is live at https://janfredrics.github.io/pineapple-run/ | medium | Needs a human with a phone |
| R3 | S0 | ScoreBook eviction UX nuances: a just-played low-rated level can be evicted next; tie-break is "least recently improved" not "least recently played". | low | Cosmetic; revisit if score UX matters in S5 |
| R4 | S0 | ~~`src/model/validate.ts:325` props limit is an inline `10_000` literal~~ RESOLVED S8a: `MAX_LEVEL_PROPS` in src/model/level.ts, used by validateLevelDef; boundary test (10 000 ok, 10 001 rejected). | — | done |
| R5 | S4 | Same-revision, same-structure new world is only re-drawn if the source calls `resetSource()` (documented in scene.ts; the styleguide and any S6 world switch must call it — INTEGRATION.md item 11 area). | low | Documented contract; verify call sites at S6 |
| R6 | S4 | Terrain join tolerance is coordinate-scaled for float32 transforms; beyond ~21 km real gaps under ~1 cm can be visually joined (physics unaffected). Stated bound in terrainMesh.ts. | low | Beyond practical endless distances; revisit at S9 if runs go further |
| R7 | S4 | Renderer signature memory is O(serialized manifest); bounded only because validated levels cap terrain at 200k points — `SceneManifest` itself has no renderer-enforced bound. | low | All in-repo sources are validated; note for S8 |
| R8 | S3 | An unsplittable imported span (no provably-straight cut anywhere) stays ONE chain regardless of maxChunks; memory bounded by validate's caps (100k pts/span, 200k total, 2000 spans), not by the chunk cap. | low | Bounded by input validation; note for S8 perf pass |
| R9 | S3 | Chunk extent lookup is a linear scan of recorded overhang pieces per body window. | low | Tiny in practice; revisit if profiling flags it |
| R10 | S3 | "Concave seams harmless" / "duplicate chain contacts harmful" Box2D claims rest on the fixer's measurements, not engine source (not in node_modules). | low | Behaviour pinned by physics comparison tests |
| R11 | S3 | Any chain split (even exact seams) can diverge cm-scale from a single chain in chaotic scenarios (bounces/tumbles); grows smoothly, not a catch. Physics comparison tests use non-amplifying scenarios. | low | Inherent to chunked chains; documented |
| R12 | S7 | Audio resume recovery is capped at 3 timed retries (250ms/1s/3s); after that recovery is gesture-only. A failed gesture resume while still visible waits for the NEXT gesture rather than auto-retrying. | low | Deliberate: no resume() without user interaction behind it |
| R13 | S7 | importScan tokenizer misclassifies a regex literal appearing directly after `)` or `]` as division (documented in the scanner). No such pattern exists in src/audio; scanner self-tests pin behaviour. | low | Documented limitation; revisit only if scan scope widens |
| R14 | S6 | Debris that comes to rest INSIDE the loaded terrain window persists until the cart moves away; bounded by cart part count, so not a growth risk. Long-run device profiling (frame time, WASM heap, GC over 10+ min) deferred to S8. | low | Headless test pins bounded body counts; timing needs a device |
| R15 | S6 | A destroyed run/build attempt's "Loading…" root lingers until its pending loaders settle (then removed). Cleanup of a partially-failed Pixi init is best-effort (teardown errors caught+logged). | low | Cosmetic / defensive path; fake-DOM coverage only, live check in S6V |
| R16 | S6V | No contact-event SFX (bounce/impact sounds): the physics layer exposes no contact event stream to hook audio to. | low | Needs a physics-layer event surface; consider at S8/S9 |
| R17 | S6V | Saved/share-code cart designs are not re-checked against the build area, so an oversized circle could bypass the builder's clamp. | low | Builder drag is clamped; add load-time validation at S8 |
| R18 | S6V | Dev-only window.__prAudio handle is not cleared on teardown (DEV builds only). | low | Cosmetic dev affordance |
| R19 | S6T | ~~Pineapple-wheel friction and real shock joint limits need additions to the frozen engine.ts~~ RESOLVED S8a: engine.ts opened once (header records it, then re-frozen). (1) `setPairFriction` + `MaterialDef.surface` over `b2World_SetFrictionCallback`; pineapple–wheel = 0.3 (src/physics/surfaces.ts), all other pairs bit-identical. Pinch test: a driven wheel wedged by pineapples goes from |ω| < 0.5 to > 18 rad/s. (2) `DistanceJointDef.limits` over `b2DistanceJointDef.enableLimit/minLength/maxLength`; shocks limited to 0.8–1.15× rest; the emulated bump stops (preStep + damaged-branch `applyShockStops`) are gone. Workbench-washboard V-sag test (loaded, ≥ 10/15 aboard asserted): shocks 0.78–1.18× and every speed 2.5–6 m/s crosses; its executable control (limits off, 0.9 pineapple–wheel) overruns to 1.25–1.47× and sticks at 46.5 m at 5 m/s. Audit-1: the binding holds one friction callback per module, so the engine dispatches per world via a world tag in userMaterialId (tested with interleaved and recreated worlds). springCart/springBracing unchanged. | — | done (see TUNING.md "S8a") |
| R20 | S6T | Shortcut risk (losing cargo on a fast pool jump) is real but chaotic, so unasserted; minimal cart delivers only 1-3; no M-key mute shortcut; endless opening worst dull stretch 17.9 s vs the 20 s rule. | low | Playfeel margins; revisit if playtests complain |
| R21 | S8a | Box2D joint limits are slightly soft (world joint softness; no per-joint stiffness in the binding): hard landings show up to ~3% give past a shock limit, and a wheel whose velocity is OVERWRITTEN by 14 m/s every step reaches 1.23× (spring alone 1.28×). Real play stays within ~1.18×. | low | Engine-level; the world-wide joint softness would change every joint |
| R22 | S8a | With pineapple–wheel friction 0.3, wheels no longer claw up a heap of pineapples: the S1 spike fixture cart (low front wheels) now stalls against a 15-pineapple heap it used to climb (the example cart does the opposite — the heap locked its wheels at 0.9 and it now pushes through). Rare in play (a heap only forms when the cart parks away from the funnel). | low | Deliberate trade-off of backlog #9 (≥0.4 re-locks the pinch wedge); revisit if playtests complain |
| R23 | S9 | Zero-G Tiki Bar's pool jump is a real risk: at 450 beads the pace line jumps it (2.05 s vs 4.82 s careful) but loses two pineapples there, so the careful line rates higher (70 vs 66). The pace line's losses cluster at the moon-hop exit (~x 77), the pool landing (~x 146) and the bead entry (~x 164); 12–14/15 across tiers. | low | Risk/reward by design; chaotic, so only the ≥ 2 s time gap is asserted (like R20) |
| R24 | S9 | `PhysicsWorld.step` reads every dynamic body's transform each step (render interpolation), sleeping or not: 600 sleeping beads cost ~0.46 ms/step headless (TUNING.md S9 perf). Reading only moved bodies (Box2D body move events) would need a third engine.ts opening. | low | Well inside budget headless; revisit if real-device profiling (R2/S8) flags it |
| R25 | S9 | The bead-count device tiers (≤ 2 cores / ≤ 2 GB → 300, ≤ 4 cores or unknown → 450, else 600) are a static heuristic, unverified on real phones; Safari reports no deviceMemory and clamps hardwareConcurrency, so iPhones land in mid/high by core count alone. The count is pinned per browser on its first BEAD-LEVEL load (audit-1 #1; audit-2: non-bead courses never pin), so a mis-bucketed device keeps its count until site data is cleared; there is no in-game way to change it. | medium | Needs a human with devices (with R2) |
| R26 | S9 | Beads render in front of the cart and pineapples (world layer 6, so pre-S9 layer indices stay fixed): a cart in the bead ocean reads as wading through it. Zones render behind the whole world, so their tint shows only in the air. | low | Visual choice; revisit on playtest |
| R27 | UX1 | Finish framing (src/game/framing.ts `finishWeight`) derives its ease weight from the cart's position, not from a latched state. A cart that reverses back out through the 6 m trigger zone before the blender therefore eases the frame back out, and rocking there makes the frame "breathe" (zoom and look point go in and out with the cart). This is a feel risk flagged by the UX1 round-2 audit, not a defect. Keep the position-driven ease unless playtests show it. (Numbering: main's S9 already uses R23–R26.) | low | Recorded per audit-2; if it bothers players, latch or hysteresis the weight once the cart passes the goal line |
| R28 | GL1 | A WebGL context loss mid-run re-mounts the run from the funnel (a fresh RunSession): the run in progress is lost rather than resumed on the new context. A loss during the HUD's end banner, before results are dispatched, also restarts the run, so that result is lost. Recovery is automatic twice per run screen, then the error screen with Try again. Unverified on real Safari: the fix is exercised only with fake canvases firing the DOM event; `forceContextLoss()` on a device is the live check. | low | With two contexts per page instead of one per builder visit, losses should become rare. Seamless resume would need the SceneRenderer rebuilt around a live session mid-run. |
| R29 | B1 | Blueprint theme cosmetics, accepted: (1) the blender jar's glass HANDLE stays pale cyan (only the body and rim get the `goalOutline` linework; outlining a 9 px stroke would need new art); (2) the 3 px terrain surface line thins to under 1 px below about 0.6 zoom (every theme's edge strip does this); (3) beyond the Original Course's end wall the rock band's flat `fillBelow` shows with no grid (world edge, the same in all themes). Checked live in the dev build, not on a phone. | low | Cosmetic; revisit if the owner's playtest of the original flags it |
| R30 | K1 | Kitchen's sink (5.5 m hole to a counter 0.5 m higher) needs a long purpose-built cart; the reference is the 17.5 m Kitchen Bridger (test/integration/kitchenBridger.ts). Accepted consequences: (1) the camera's 30% look-ahead cannot show the rear of any cart longer than ~14.4 m on a phone, so in Kitchen's pit the bridger's rear is off the left edge by up to 1.64 m (framing test bound 2 m). **M1 update:** the height-aware zoom widens the phone view, so the limit is now ~18.2 m on 844×390 and ~17 m on 1280×720. The bridger's rear overhang is 0.19 m at 844×390, 0 at 844×320 and 0.75 m at 1280×720 (unchanged, now the worst case); the bound is tightened to 1 m. The same holds on the original's new finish (worst 0.92 m at 1280×720, holding right). With K1's camera, the blender was only fully on screen once the bridger's front was within ~4.3 m of it (its right edge up to ~6 m off before that), and the bridger's 5.2 m-tall frame started out under the top HUD pad. **M1 update** (height-aware zoom, re-measured on Kitchen's pace line and on the original at 10 m/s and floored): the whole blender is on screen once the front is within ~9.0 m on 844×390, through the whole pit window on 844×320, and within 7.6–7.9 m on 1280×720, now the worst case. Before that, its right edge is up to 1.5–1.7 m off on 844×390 and up to 3.0 m off on 1280×720. The frame is not under the top HUD pad at any step in the pit window. Its framing test is a separate, named case (front half over the last 4.75 m); normal carts keep the full-strength check. K1 left the camera unchanged; M1 changed only the follow zoom (TUNING.md "M1"). (2) Kitchen's pit landing strip is 20 m (`KITCHEN_FINISH = { frontGap: 20, lineGap: 10.5 }`), not the shared 5.75 m, so a 17.5 m cart gets its bed into the goal sensor. The goal line and sensor sit 10.5 m before the blender (not at the lip), so a normal cart scores only where the camera shows the whole blender. The first ~9.5 m of the pit floor is therefore a landing counter where pineapples don't count, and everything past the line does, even ~10 m short of the blender. The scoring rule is the same on every course (delivered = past `goal.lineX` by x; src/model/runEvents.ts), but Kitchen's line is 10.5 m from the blender, not ~7 m. A line nearer the blender was measured and rejected: the bridger's load rests 5.5–9.2 m before it, so a 2–4 m line gives it 0–1/15. A floored bridger delivers 9/15 at 50 (11/15 at 62 with the line at the lip): at the count, two pineapples ride in the rear of its bed, before the line. (7) The sink can be jumped with a launch lip: in the synthetic sweep, a 0.3 m hump within 4 m of the edge, or a 0.6 m hump or kicker within 8 m, crosses at 10–14 m/s and up. Kitchen has none; kitchen.test.ts pins its run-up level for 12 m. (3) The bridger rolls through the drainer pool and never flies it, so Kitchen's shortcut is a faster roll-through (3.90 vs 5.60 s), not a jump. (4) Above ~9 m/s the bridger's crossing is chaotic (steady 9.5 m/s stalls at the sink with the stuck hint; 12 m/s delivers 9/15). (5) StuckDetector (5 s / 0.3 m) can miss a cart rocking ±0.3 m while wedged; seen at the sink before the 12 m landing flat fixed that geometry, and not reproduced on the shipped course. (6) Progression: course 3 (Workbench) is again doable with the example cart after course 2 (Kitchen) required a build. | low | (Numbered R30: main's B1 already uses R29.) Revisit on playtest: a look-ahead that shrinks for long carts, a gentler long-cart speed band, or reordering Kitchen after Workbench. Detector: add a rocking check (net progress over a longer window) if it shows up in play. |
| R31 | M1 | Accepted M1 quirks. (1) The original now has Kitchen's finish (`ORIGINAL_FINISH = { frontGap: 20, lineGap: 10.5 }`): the first ~9.5 m of its pit floor is a landing strip where pineapples don't count, and its goal line is 10.5 m before the blender (7971.75 px), not the 2008 line at the lip (7670 px, kept as `ORIGINAL_GOAL_LINE_PX`). (2) Failing example-cart lines on the original no longer pick up a pineapple or two that fell into the short pit: steady 10 m/s scores 4/15 → 2/15, and lines that jam end on the stuck hint. The expert line is unchanged (10/15, 52). (3) The Kitchen Bridger on the original stalls on the terrain at steady 6 and 8 m/s (111 / 61 m before the blender), before the finish; 7 and 9–12 m/s and holding right finish with 12–15/15. (4) The follow zoom now depends on the viewport height, so an iOS toolbar collapse or expand mid-run changes the zoom in one step (844×390 0.929 ↔ 844×320 0.762); no easing. (5) The dvh sizing, the ResizeObserver resize and the safe-area insets were verified only in the desktop browser pane with emulated sizes, not on a real iPhone in Safari. (6) Two dev pages keep the old width-only zoom `clamp(w / 720, 0.5, 1.5)`: the run harness (src/run/harness.ts) and the S0 physics spike (src/spike/page.ts). The styleguide uses fixed zooms. None of them is the game; they were left as they are so the spike stays the S0 reference. | low | Needs a real-phone check (Jan); ease the zoom change if the toolbar jump bothers in play. |
